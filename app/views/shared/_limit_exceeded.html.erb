<div class="
  mt-10
  mb-16
  max-w-4xl mx-auto
  p-8
  rounded-2xl
  bg-white
  shadow-xl
  border border-blue-100
  text-center
">

  <h2 class="text-2xl font-bold text-blue-800 mb-4">
    本日の利用回数に達しました
  </h2>

  <% if user_signed_in? %>

    <p class="text-gray-700">
      本日は
      <span class="font-bold text-blue-700">
        <%= Chatgpt::UsageLimit::USER_DAILY_LIMIT %>回
      </span>
      に達したため、これ以上ご利用いただけません。
    </p>

    <p class="mt-2 text-gray-500">
      また明日ご利用ください。
    </p>

  <% else %>

    <p class="text-gray-700">
      未ログインの場合は1日
      <span class="font-bold text-blue-700">
        <%= Chatgpt::UsageLimit::GUEST_DAILY_LIMIT %>回
      </span>
      まで利用できます。
    </p>

    <p class="mt-2 text-gray-700">
      ログインすると1日
      <span class="font-bold text-blue-700">
        <%= Chatgpt::UsageLimit::USER_DAILY_LIMIT %>回
      </span>
      まで利用できます。
    </p>

    <div class="mt-6">
      <%= link_to "Googleでログインして続ける",
          user_google_oauth2_omniauth_authorize_path,
          class: "
            inline-block
            px-6 py-3
            rounded-full
            bg-card-gradient
            text-white
            font-semibold
            shadow-lg
            hover:opacity-90
            transition
          " %>
    </div>

  <% end %>
</div>